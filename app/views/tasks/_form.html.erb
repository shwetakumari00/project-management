<%= form_for ([@task.project, @task]) do |form| %> 

<% if @task.errors.any? %>
  <h2>
    <%= pluralize(@task.errors.count,"error") %>Prohibited this Task form being saved:
  </h2>
  <ul> 
    <% @task.errors.full_messages.each do |message| %>
      <li><%= message %></li>
    <% end %>
  </ul>
<% end %>


  <div class='form-group'>
    <%= form.label :name %><br>
    <%= form.text_field :name,class:'form-control' %><br>
  </div>

  <div class='form-group'>
    <%= form.label :description %><br>
   <%= form.text_field :description,class:'form-control' %><br>
  </div><br>
 
 <!-- <div>
    <%#= form.label :status %>
    <%#= select_tag :status, options_for_select(Task.statuses.map{ |key, value| [key.humanize, key] })%>
  </div>
  -->
  <div>
    <%= form.label :status %>
   <%= form.select :status, Task::STATUS_OPTION, {:prompt => true}, class: 'form-control' %>
    </div> 

 <div><br>
    <%= form.submit class:"btn btn-primary"%>
  </div>
<% end %>
